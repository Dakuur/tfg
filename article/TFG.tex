\documentclass[10pt,a4paper,twocolumn,twoside]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[catalan]{babel}
\usepackage{multicol}
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{times}
\usepackage{titlesec}
\usepackage{multirow}
\usepackage{lettrine}
\usepackage[backend=biber, style=numeric, sorting=none]{biblatex}
\usepackage[top=2cm, bottom=1.5cm, left=2cm, right=2cm]{geometry}
\usepackage{url}
\usepackage{float}
\usepackage[figurename=Fig.,tablename=TAULA]{caption}
\usepackage{booktabs}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{xcolor}
\usepackage{colortbl}
% \usepackage{parskip}
\addbibresource{references.bib}
\captionsetup[table]{textfont=sc}
\usepackage[hidelinks]{hyperref}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}

\author{\LARGE\sffamily David Morillo Massagué}
\title{\Huge{\sffamily Predicció de Metàstasis en Histopatologia en Càncer Colorrectal usant

Graph Attention Networks}}

\date{}

\newcommand\blfootnote[1]{%
  \begingroup
  \renewcommand\thefootnote{}\footnote{#1}%
  \addtocounter{footnote}{-1}%
  \endgroup
}

\titleformat{\section}
{\large\sffamily\scshape\bfseries}
{\textbf{\thesection}}{1em}{}

\begin{document}

\fancyhead[LO]{\scriptsize David Morillo Massagué: Predicció de Metàstasi en Histopatologia usant GATs}
\fancyhead[RO]{\thepage} \fancyhead[LE]{\thepage}
\fancyhead[RE]{\scriptsize EE/UAB TFG DADES: Predicció de Metàstasi en Histopatologia usant GATs}

\fancyfoot[CO,CE]{}

\fancypagestyle{primerapagina}
{
   \fancyhf{}
   \fancyhead[L]{\scriptsize TFG EN ENGINYERIA DADES, ESCOLA D'ENGINYERIA (EE), UNIVERSITAT AUTÒNOMA DE BARCELONA (UAB)}
   \fancyfoot[C]{\scriptsize Juliol de 2026, Escola d'Enginyeria (UAB)}
}

\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\pagestyle{fancy}

\twocolumn[\begin{@twocolumnfalse}

\maketitle

\thispagestyle{primerapagina}

\begin{center}
\parbox{0.915\textwidth}
{\sffamily
\textbf{Resum--}
Aquest treball aborda la predicció de metàstasis en ganglis limfàtics en pacients amb càncer colorectal en estadi
pT1 a partir d'imatges de gran format (\textit{Whole Slide Images}). S'utilitzen \textit{Graph Attention Networks}
(GAT) sobre grafs on cada node representa un \textit{bag} de 256 \textit{patches} de $256{\times}256$\,px
($\approx4096{\times}4096$\,px al WSI), caracteritzat per l'embedding CLS de 1.536 dimensions del model de
fundació \textit{UNI2}. S'exploren dues estratègies de \textit{pooling}: jeràrquic (DiffPool intercalat entre capes GAT, $N\to K_1\to K_2$ nodes) o pla (\textit{mean}/\textit{max} al final). El conjunt de dades comprèn 963 grafs provinents de 230 pacients, dividits
estratificadament en entrenament i validació. Per afrontar el desbalanceig de classes (N0\,$\gg$\,N1), s'apliquen
pesos per classe. Donat que el diagnòstic és per pacient, s'implementen diverses estratègies
d'agregació (\textit{Multiple Instance Learning}, MIL) que integren les prediccions de totes les làmines d'un
pacient. Un \textit{grid search} inicial ha identificat un model candidat amb AUC de 0{,}846. A més, s'ha
desenvolupat un \textit{frontend} web per a la inferència en temps real i la visualització de l'atenció del model.
\\
\\
\textbf{Paraules clau-- } Càncer colorectal, Histopatologia digital, Graph Attention Networks (GAT), DiffPool,
Metàstasi, Estadi pT1, Aprenentatge per múltiples instàncies (MIL), Desbalanceig de classes.\\
\\
\bigskip
\\
\textbf{Abstract--}
This work addresses lymph node metastasis prediction in pT1 colorectal cancer patients from Whole Slide Images.
We propose a methodology based on Graph Attention Networks (GAT) where each node represents a bag of
256 patches of $256{\times}256$\,px ($\approx4096{\times}4096$\,px at WSI level 0), characterised by a
1,536-dimensional CLS embedding from the UNI2 foundation model. Two pooling strategies are explored: hierarchical DiffPool layers
\emph{interleaved between} GAT layers ($N\!\to\!K_1\!\to\!K_2$ nodes), enabling multi-scale
tissue representations, or flat readout (mean/max) applied after the final GAT layer. The dataset comprises 963 graphs from 230 patients, stratified into training and
validation splits. To handle class imbalance (N0\,$\gg$\,N1), class weighting is applied.
Since the diagnosis is patient-level rather than slide-level, Multiple Instance Learning (MIL) aggregation
strategies are implemented to integrate predictions across all slides of a patient. An initial grid search has
identified a candidate model achieving an AUC of 0.846. An interactive web frontend has also been developed for
real-time inference and attention visualization.
\\
\\
\textbf{Keywords-- } Colorectal cancer, Digital histopathology, Graph Attention Networks (GAT), DiffPool,
Metastasis, pT1 stage, Multiple Instance Learning (MIL), Class imbalance.\\
}

\bigskip

{\vrule depth 0pt height 0.5pt width 4cm\hspace{7.5pt}%
\raisebox{-3.5pt}{\fontfamily{pzd}\fontencoding{U}\fontseries{m}\fontshape{n}\fontsize{11}{12}\selectfont\char70}%
\hspace{7.5pt}\vrule depth 0pt height 0.5pt width 4cm\relax}

\end{center}

\bigskip
\end{@twocolumnfalse}]

\blfootnote{$\bullet$ E-mail de contacte: David.MorilloMa@autonoma.cat}
\blfootnote{$\bullet$ Treball tutoritzat per: Debora Gil Resina (DCC)} \blfootnote{$\bullet$ Curs 2025/26}

\section{Introducció}

\lettrine[lines=3]{E}{l} càncer colorectal (CCR) representa un dels reptes de salut pública més greus del nostre
entorn. A Espanya, la SEOM preveu per al 2026 més de \textbf{44.132} nous casos anuals, convertint-lo en el tumor
més freqüentment diagnosticat i en la segona causa de mort per càncer amb 14.629 defuncions \cite{seom2026}.

El carcinoma pT1 és especialment complex: el tumor ha envaït la submucosa però no la muscular pròpia, i el risc de
metàstasi als ganglis limfàtics oscil·la entre el \textbf{2\%} i el \textbf{23\%} depenent de la profunditat
d'invasió \cite{ParcSalutMar2013}. Quan hi ha criteris de mal pronòstic, el risc s'estima al voltant del 10\%, la
qual cosa obliga a considerar una cirurgia radical. Tanmateix, s'estima que \textbf{entre el 80\% i el 90\%}
d'aquestes cirurgies resulten innecessàries \cite{MartinezDeJuan2024}.

La histopatologia digital, a través de les \textit{Whole Slide Images} (WSI), obre la porta a tècniques avançades
de visió per computador. En concret, les \textit{Graph Attention Networks} (GAT) \cite{veličković2018graphattentionnetworks}
permeten modelar l'arquitectura espacial del teixit de forma relacional. Aquest projecte, desenvolupat al grup
IAM del CVC (iam.cvc.uab.es) sota la supervisió de la Dra.\ Debora Gil, proposa un \textit{pipeline} complet per a
la classificació N0/N1 que integra construcció de grafs, \textit{pooling} jeràrquic diferenciable i agregació MIL
per pacient.

\section{Motivació}

Dues raons complementàries justifiquen la tria d'aquest projecte. Des del punt de vista clínic, els sistemes d'IA
demostren una capacitat creixent per identificar patrons morfològics subtils de manera consistent, amb el potencial
real de reduir cirurgies innecessàries. Des del punt de vista tècnic, la combinació de xarxes d'atenció sobre grafs
i aprenentatge profund sobre imatges histopatològiques d'alta resolució és exactament la intersecció entre la teoria
de grafs i les xarxes neuronals profundes que vull explorar dins de l'Enginyeria de Dades.

\section{Objectius}

\begin{itemize}
    \item Construir un \textit{pipeline} complet de classificació N0/N1 sobre WSI reals a partir d'embeddings CLS.
    \item Avaluar estratègies de \textit{pooling} jeràrquic per a la representació global de grafs histopatològics.
    \item Implementar i comparar mètodes d'agregació MIL a nivell de pacient.
    \item Explorar l'ús de les màscares d'anotació expert per supervisar l'atenció del model.
    \item Proporcionar una eina interactiva de visualització i inferència en temps real.
\end{itemize}

\section{Dades i Preprocessament}

Aquest projecte es realitza en col·laboració amb un equip de la UAB que cobreix des de la recollida de dades fins
a la creació d'autoencoders per a l'extracció de característiques.

\subsection{Imatges de Gran Format (WSI)}

Les dades originals consisteixen en imatges de làmines senceres (WSI) obtingudes amb escàners d'alta resolució i
tintades amb hematoxilina-eosina, emmagatzemades en format MIRAX (\textit{.mrxs}). Les dimensions oscil·len entre
els 30.000 i els 150.000 píxels per costat, amb una quantitat significativa d'espai buit o artefactes que
requereixen tractament previ.

El conjunt de dades comprèn \textbf{963 grafs}, un per secció histològica (cadascun amb una o diverses components
connexes), provinents de \textbf{230 pacients} en total. S'aplica una divisió estratificada 80/20 a nivell de
pacient per evitar la filtració d'informació entre entrenament i validació.

\subsection{Anotacions i Màscares de Segmentació}

La Dra.\ Eva Musulén, patòloga especialitzada, ha realitzat les anotacions manualment amb el programari
\textit{Labelme}. Aquestes identifiquen tres categories d'afectació:

\begin{itemize}
    \item \textbf{Infiltració:} teixit anormal que envaeix zones no corresponents.
    \item \textbf{Displàsia:} cèl·lules anormals que poden esdevenir canceroses.
    \item \textbf{Infiltració + Displàsia:} emprat en casos de dubte diagnòstic.
\end{itemize}

A partir d'aquestes s'obtenen màscares de segmentació codificades per color: vermell (infiltració), blau
(displàsia) i verd clar (combinada).

\begin{figure}[h]
    \centering
    \includegraphics[width=0.45\textwidth]{img/mascara.png}
    \caption{Exemple de WSI i la seva corresponent màscara de segmentació.}
    \label{fig-mask}
\end{figure}

\subsection{Extracció de Patches i Embeddings CLS}

S'aplica una finestra lliscant (\textit{sliding window}) sense solapament per dividir cada làmina en
\textit{patches} de $256 \times 256$ píxels. Aquests s'agrupen en \textbf{bags} de $16 \times 16$
\textit{patches} (256 \textit{patches} per \textit{bag}), cadascun cobrint una regió de
$\approx 4096 \times 4096$ píxels del WSI a resolució completa (nivell 0). El model de fundació
\textbf{UNI2}~\cite{chen2024uni2} processa cada \textit{bag} i extreu el token \textit{CLS} de la
seva sortida, obtenint un vector de \textbf{1.536 dimensions} que condensa la informació morfològica
i textural de la regió. No s'aplica cap filtratge de qualitat previ (ni per borrositat ni per
proporció de blanc): tots els \textit{bags} d'una secció generen un node del graf.

\section{Construcció del Graf}

\subsection{Nodes i característiques}

Cada \textit{bag} (conjunt de 256 \textit{patches} de $256{\times}256$\,px que cobreix
$\approx4096{\times}4096$\,px al WSI) constitueix un \textbf{node} del graf. El vector de
característiques de cada node és l'embedding CLS de \textbf{1.536 dimensions} obtingut per UNI2.
La posició del node al pla WSI és el centroide de les coordenades dels seus 256 \textit{patches}.
A la subfigura~\ref{fig:patches} es pot observar la distribució esparsa dels \textit{bags}
extrets sobre una làmina real.

\subsection{Model de dades: dimensions i estructures}
\label{sec:model-dades}

Formalment, cada secció histològica es representa com un graf
$\mathcal{G} = (\mathbf{X},\,\mathbf{A})$ on:
\begin{itemize}
  \item $\mathbf{X} \in \mathbb{R}^{N \times 1536}$ és la matriu de característiques dels $N$ nodes
        (\textit{bags}).
  \item $\mathbf{A} \in \{0,1\}^{N \times N}$ és la matriu d'adjacència construïda per triangulació
        de Delaunay i posterior poda d'arestes llargues.
\end{itemize}

Dues estratègies de \textit{pooling} transformen la seqüència de grafs en un vector global que
alimenta el capçal MLP per produir $P(\text{N1})$:
\begin{itemize}
  \item \textbf{Pla (\textit{readout}).} \textit{Mean} o \textit{max} aplicats sobre les sortides de
  la darrera capa GAT, reduint directament de $N$ nodes a $d_h$ dimensions.
  \item \textbf{Jeràrquic (DiffPool intercalat).} Dues capes DiffPool s'insereixen entre les capes
  GAT: la primera colapsa el graf de $N$ nodes a $K_1$ super-nodes, i la segona de $K_1$ a $K_2$.
  Cada nivell aprèn una matriu d'assignació suau i produeix un nou graf de menor grandària sobre el
  qual opera la capa GAT següent.
\end{itemize}

\textbf{Model 1 --- Múltiples grafs + agregació MIL.}
Cada secció genera un graf independent; les $S$ seccions d'un pacient es processen per separat i les
probabilitats $\{P(\text{N1}_i)\}_{i=1}^{S}$ s'agreguen a posteriori (Secció~\ref{sec:mil}).

\textbf{Model 2 --- Mega-graf pacient (un sol graf).}
Com a alternativa, es pot construir un únic graf per pacient concatenant verticalment les matrius de
característiques i col·locant les matrius d'adjacència en la diagonal d'una mega-matriu per blocs:

\begin{align}
  \mathbf{X}_{\text{pac}} &=
  \begin{pmatrix}
    \mathbf{X}_1 \\
    \vdots \\
    \mathbf{X}_S
  \end{pmatrix}
  \in \mathbb{R}^{\left(\sum_i N_i\right) \times 1536} \\[10pt]
  \mathbf{A}_{\text{pac}} &=
  \begin{pmatrix}
    \mathbf{A}_1 & \mathbf{0} & \cdots \\
    \mathbf{0}   & \ddots     & \mathbf{0} \\
    \cdots       & \mathbf{0} & \mathbf{A}_S
  \end{pmatrix}
\end{align}

Els triangles fora de la diagonal queden a zero, és a dir, no hi ha arestes entre seccions
diferents. D'aquesta manera el mecanisme d'atenció del GAT opera sobre totes les seccions
simultàniament, i un únic \textit{pooling} global produeix directament el diagnòstic del pacient
sense necessitat d'una etapa d'agregació de probabilitats. La implementació actual correspon al
Model 1; el Model 2 és una línia de treball futura.

\subsection{Connectivitat per triangulació de Delaunay}

Una connectivitat fixa de reixeta no és adequada perquè la màscara exclou regions de fons, trencant la simetria
de la quadrícula inicial. Per aquest motiu, s'aplica una \textbf{triangulació de Delaunay} sobre els nodes
restants, que s'adapta orgànicament a distribucions irregulars. A continuació, s'eliminen les arestes que creuen
píxels de valor zero en la màscara, evitant connexions entre regions separades per espai buit. Finalment,
s'aplica un criteri de distància màxima: s'eliminen les arestes que superen el doble de la longitud mitjana del
conjunt (\texttt{DISTANCE\_FACTOR} $= 2{,}0$). La subfigura~\ref{fig:delaunay-real} mostra el resultat sobre
la mateixa làmina: les arestes eliminades pel filtre de màscara apareixen en vermell.

\begin{figure*}[h]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[height=7.5cm, keepaspectratio]{img/extracted_patches.png}
        \caption{\textit{Patches} extrets (nodes del graf) superposats sobre la imatge original.}
        \label{fig:patches}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\textwidth}
        \centering
        \includegraphics[height=7.5cm, keepaspectratio]{img/delaunay_graph.png}
        \caption{Graf de Delaunay sobre la imatge anotada. Les zones verdes indiquen àrees afectades anotades
        per la patòloga; les arestes en vermell han estat eliminades pel filtratge de màscara.}
        \label{fig:delaunay-real}
    \end{subfigure}
    \caption{Visualització del procés de construcció del graf sobre una làmina real. Les dues imatges corresponen
    a la mateixa secció histològica i il·lustren com la topologia del graf s'adapta a la distribució irregular
    del teixit.}
    \label{fig:patches-delaunay}
\end{figure*}

\section{Arquitectura del Model}

\subsection{GATClassifier}

El model implementat, \texttt{GATClassifier}, és un classificador de grafs que alterna capes
\textbf{Graph Attention Network} (\texttt{GATConv}~\cite{veličković2018graphattentionnetworks,fey2019fastgraphrepresentationlearning})
amb capes de \textit{pooling} jeràrquic per a la classificació binària N0/N1. L'arquitectura proposa
intercalar operacions de reducció del graf \textit{entre} les capes de propagació de missatges, de
manera que cada estadi treballa sobre una representació progressivament més compacta del teixit:

\begin{equation*}
  \underbrace{\mathbf{X}^{(0)},\,\mathbf{A}^{(0)}}_{N\text{ nodes}}
  \xrightarrow{\text{GAT}_1}
  \xrightarrow{\text{Pool}_1}
  \underbrace{\mathbf{X}^{(1)},\,\mathbf{A}^{(1)}}_{K_1\text{ nodes}}
  \xrightarrow{\text{GAT}_2}
  \xrightarrow{\text{Pool}_2}
  \underbrace{\mathbf{X}^{(2)},\,\mathbf{A}^{(2)}}_{K_2\text{ nodes}}
  \xrightarrow{\text{GAT}_3}
  \xrightarrow{\text{readout}}
  \mathbf{z}
\end{equation*}

\noindent on $K_1 \ll N$ i $K_2 \ll K_1$ són el nombre de \textit{super-nodes} de cada nivell jeràrquic.

\textbf{Mecanisme d'atenció.} A diferència de les xarxes convolucionals clàssiques sobre grafs que tracten
tots els veïns d'un node de forma uniforme, GAT aprèn a assignar pesos d'importància diferenciats a cada
veí. Donada la representació $\mathbf{h}_i \in \mathbb{R}^{F}$ del node $i$, per a cada parella de nodes
veïns $(i, j)$ es calcula un coeficient d'atenció no normalitzat:
\begin{equation*}
  e_{ij} = \text{LeakyReLU}\!\left(\mathbf{a}^{\top}\!\left[\mathbf{W}\mathbf{h}_i \;\|\; \mathbf{W}\mathbf{h}_j\right]\right)
\end{equation*}
on $\mathbf{W}$ és una transformació lineal aprenible, $\|$ la concatenació i $\mathbf{a}$ un vector
d'atenció aprenible. Els coeficients es normalitzen amb \textit{softmax} sobre tots els veïns del node $i$:
\begin{equation*}
  \alpha_{ij} = \frac{\exp(e_{ij})}{\displaystyle\sum_{k \in \mathcal{N}(i)} \exp(e_{ik})}
\end{equation*}
i la nova representació del node s'obté com la suma ponderada dels veïns:
\begin{equation*}
  \mathbf{h}'_i = \sigma\!\left(\sum_{j \in \mathcal{N}(i)} \alpha_{ij}\,\mathbf{W}\mathbf{h}_j\right)
\end{equation*}

\textbf{Multi-cap.} Per incrementar la capacitat expressiva, cada capa usa $H$ caps d'atenció independents
les sortides dels quals es concatenen (a les dues primeres capes) o es promitgen (tercera capa). D'aquesta
manera el model pot capturar múltiples tipus de relació de veïnatge simultàniament. Cada capa va
seguida de normalització per \textit{batch}, activació ELU i \textit{dropout}.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.45\textwidth]{img/gat}
    \caption{Il·lustració del mecanisme d'atenció en una capa GAT amb tres caps independents (verd, blau,
    lila). El node central $\vec{h}_1$ agrega les representacions dels seus veïns i la seva pròpia
    ($\alpha_{11}$, \textit{self-loop}) mitjançant coeficients d'atenció $\alpha_{1j}$ apresos per cada
    cap. Les sortides dels caps es combinen per concatenació o promig per obtenir la nova representació
    $\vec{h}'_1$.}
    \label{fig:gat}
\end{figure}

El capçal MLP (\textit{Linear}~$\to$~ReLU~$\to$~\textit{Dropout}~$\to$~\textit{Linear}) transforma la
representació global del graf en els logits N0/N1.

\subsection{Estratègies de Pooling Pla (\textit{Readout})}

Quan no s'utilitza pooling jeràrquic, les representacions dels nodes de la darrera capa GAT s'agreguen
directament amb operacions de \textit{readout}: \textit{mean}, \textit{max} o la seva concatenació
(\texttt{mean\_max}). Tot i la seva eficiència computacional, aquestes estratègies ignoren la topologia del
graf i col·lapsen tota la informació espacial en un sol pas.

\subsection{Pooling Jeràrquic: DiffPool entre Capes GAT}

Per superar les limitacions del \textit{readout} pla, s'implementa \textbf{DiffPool}
\cite{ying2019hierarchicalgraphrepresentationlearning} com a operació intermèdia entre les capes de
propagació. A diferència d'aplicar el \textit{pooling} únicament al final, la proposta intercala dues
capes DiffPool al llarg de la xarxa: una entre la primera i la segona capa GAT, i una altra entre la
segona i la tercera. Cada capa DiffPool aprèn una matriu d'assignació suau (\textit{soft assignment})
$\mathbf{S} \in \mathbb{R}^{N \times K}$ que agrupa els $N$ nodes actuals en $K \ll N$
\textit{super-nodes}:
\begin{equation*}
  \mathbf{X}^{(\ell+1)} = \mathbf{S}^{\top}\mathbf{X}^{(\ell)}, \qquad
  \mathbf{A}^{(\ell+1)} = \mathbf{S}^{\top}\mathbf{A}^{(\ell)}\mathbf{S}
\end{equation*}

Amb aquesta arquitectura, la primera capa GAT opera sobre el graf original de nodes de teixit
($N$ bags), la segona ho fa sobre un graf reduït de $K_1$ super-nodes que representen regions de teixit
de nivell intermedi, i la tercera opera sobre un graf molt compacte de $K_2$ super-nodes que codifiquen
l'organització arquitectònica global de la secció. Finalment, un \textit{readout} global (\textit{mean}
o \textit{max}) sobre els $K_2$ nodes produeix l'embedding que entra al capçal MLP.

L'entrenament incorpora una pèrdua auxiliar (\textit{link prediction} + entropia mínima) per
regularitzar l'assignació i evitar degeneracions trivials.

\begin{figure*}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{img/hierarchical_pool.png}
    \caption{Agrupació jeràrquica de nodes per \textit{DiffPool}: l'assignació suau colapsa progressivament el
    graf en super-nodes de major abstracció. En el model proposat, dos nivells DiffPool s'intercalen entre les
    capes GAT. Font: \cite{ying2019hierarchicalgraphrepresentationlearning}.}
    \label{fig-diffpool}
\end{figure*}

Com a alternativa, \textbf{SAGPool} \cite{lee2019selfattentiongraphpooling} proposa un enfocament esparç que
selecciona els $K$ nodes més rellevants via puntuacions d'auto-atenció, descartant la resta i preservant
l'estructura topològica original. A diferència de DiffPool, SAGPool no aprèn assignacions suaus sinó
seleccions discretes, amb un cost computacional menor però sense la capacitat de capturar representacions
jeràrquiques continues. En aquest projecte s'utilitza DiffPool per la seva major expressivitat i la
possibilitat d'integrar la pèrdua auxiliar de regularització.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance = 0.30cm,
  % ── base box ──────────────────────────────────────────────────
  B/.style = {rectangle, rounded corners=3pt, minimum width=5.2cm,
              minimum height=0.70cm, align=center, font=\small,
              draw=#1!65!black, fill=#1!9, line width=0.9pt},
  % norm row (slimmer)
  N/.style = {rectangle, rounded corners=2pt, minimum width=5.2cm,
              minimum height=0.48cm, align=center, font=\scriptsize\itshape,
              draw=gray!40, fill=gray!6, line width=0.7pt},
  % annotations
  dim/.style = {font=\scriptsize\ttfamily, text=black!40, anchor=west},
  sec/.style = {font=\scriptsize\bfseries\sffamily, text=black!45, anchor=east},
  arr/.style = {->, >=stealth, line width=0.85pt, draw=black!40},
  aux/.style = {font=\scriptsize\itshape, text=orange!70!black},
]

%% ── INPUT ─────────────────────────────────────────────────────
\node[B=gray] (inp)
  {$\mathbf{X}\!\in\!\mathbb{R}^{N\times 1536}$,\enspace
   $\mathbf{A}\!\in\!\{0,1\}^{N\times N}$};
\node[dim, right=0.22cm of inp] {$N\!\times\!1536$};

%% ── GAT LAYER 1 ───────────────────────────────────────────────
\node[B=blue, below=0.38cm of inp] (g1)
  {GATConv$_1$\enspace $1536\!\to\!128$,\enspace $H\!=\!4$, concat};
\node[N, below=0.15cm of g1] (n1)
  {BatchNorm\;·\;ELU\;·\;Dropout};
\node[dim, right=0.22cm of n1] {$N\!\times\!512$};

%% ── DIFFPOOL 1 ────────────────────────────────────────────────
\node[B=orange, below=0.38cm of n1] (p1)
  {DiffPool$_1$\enspace $N \longrightarrow K_1$ super-nodes};
\node[dim, right=0.22cm of p1] {$K_1\!\times\!128$};
% aux loss annotation
\node[aux, right=0.22cm of p1, yshift=-0.55cm] (al1)
  {$+\;\mathcal{L}_{\text{link}} + \mathcal{L}_{\text{ent}}$};
\draw[->, >=stealth, dashed, orange!45, line width=0.6pt]
  (p1.south east) to[out=-10,in=180] (al1.west);

%% ── GAT LAYER 2 ───────────────────────────────────────────────
\node[B=blue, below=0.55cm of p1] (g2)
  {GATConv$_2$\enspace $128\!\to\!128$,\enspace $H\!=\!4$, concat};
\node[N, below=0.15cm of g2] (n2)
  {BatchNorm\;·\;ELU\;·\;Dropout};
\node[dim, right=0.22cm of n2] {$K_1\!\times\!512$};

%% ── DIFFPOOL 2 ────────────────────────────────────────────────
\node[B=orange, below=0.38cm of n2] (p2)
  {DiffPool$_2$\enspace $K_1 \longrightarrow K_2$ super-nodes};
\node[dim, right=0.22cm of p2] {$K_2\!\times\!128$};
\node[aux, right=0.22cm of p2, yshift=-0.55cm] (al2)
  {$+\;\mathcal{L}_{\text{link}} + \mathcal{L}_{\text{ent}}$};
\draw[->, >=stealth, dashed, orange!45, line width=0.6pt]
  (p2.south east) to[out=-10,in=180] (al2.west);

%% ── GAT LAYER 3 ───────────────────────────────────────────────
\node[B=blue, below=0.55cm of p2] (g3)
  {GATConv$_3$\enspace $128\!\to\!128$,\enspace $H\!=\!1$, mean};
\node[N, below=0.15cm of g3] (n3)
  {BatchNorm\;·\;ELU};
\node[dim, right=0.22cm of n3] {$K_2\!\times\!128$};

%% ── READOUT ───────────────────────────────────────────────────
\node[B=teal, below=0.38cm of n3] (ro)
  {Readout\enspace mean\,$\Vert$\,max\enspace over $K_2$ nodes};
\node[dim, right=0.22cm of ro] {$256$};

%% ── MLP ───────────────────────────────────────────────────────
\node[B=violet, below=0.38cm of ro] (m1)
  {Linear$_{256\to 128}$\;·\;ReLU\;·\;Dropout};
\node[B=violet, below=0.15cm of m1] (m2)
  {Linear$_{128\to 2}$};
\node[dim, right=0.22cm of m2] {$2$};

%% ── OUTPUT ────────────────────────────────────────────────────
\node[B=green!60!black, below=0.38cm of m2] (out)
  {Softmax\enspace$P(\text{N0}),\enspace P(\text{N1})$};

%% ── ARROWS ────────────────────────────────────────────────────
\foreach \a/\b in {inp/g1, g1/n1, n1/p1, p1/g2, g2/n2, n2/p2,
                   p2/g3, g3/n3, n3/ro, ro/m1, m1/m2, m2/out}
  \draw[arr] (\a)--(\b);

%% ── LEFT SECTION LABELS ───────────────────────────────────────
\draw[gray!30, line width=0.6pt]
  ([xshift=-0.45cm]g1.north west) -- ([xshift=-0.45cm]n1.south west);
\node[sec] at ([xshift=-0.5cm]$(g1.west)!0.5!(n1.west)$) {\rotatebox{90}{GAT 1}};

\node[sec] at ([xshift=-0.5cm]p1.west) {\rotatebox{90}{Pool 1}};

\draw[gray!30, line width=0.6pt]
  ([xshift=-0.45cm]g2.north west) -- ([xshift=-0.45cm]n2.south west);
\node[sec] at ([xshift=-0.5cm]$(g2.west)!0.5!(n2.west)$) {\rotatebox{90}{GAT 2}};

\node[sec] at ([xshift=-0.5cm]p2.west) {\rotatebox{90}{Pool 2}};

\draw[gray!30, line width=0.6pt]
  ([xshift=-0.45cm]g3.north west) -- ([xshift=-0.45cm]n3.south west);
\node[sec] at ([xshift=-0.5cm]$(g3.west)!0.5!(n3.west)$) {\rotatebox{90}{GAT 3}};

\node[sec] at ([xshift=-0.5cm]ro.west)  {\rotatebox{90}{Pool}};

\draw[gray!30, line width=0.6pt]
  ([xshift=-0.45cm]m1.north west) -- ([xshift=-0.45cm]m2.south west);
\node[sec] at ([xshift=-0.5cm]$(m1.west)!0.5!(m2.west)$) {\rotatebox{90}{MLP}};

\end{tikzpicture}
\caption{Arquitectura detallada del \texttt{GATClassifier} amb \textit{pooling} jeràrquic. Les etiquetes
a la dreta indiquen la forma del tensor de sortida de cada bloc ($h\!=\!128$, $H\!=\!4$, per tant
$h{\cdot}H\!=\!512$). Les capes DiffPool aprenen una matriu d'assignació suau $\mathbf{S}$ i emeten una
pèrdua auxiliar $\mathcal{L}_{\text{aux}}$ que es suma a la BCE principal. $K_1 \ll N$ i $K_2 \ll K_1$
són els super-nodes dels dos nivells de reducció (p.~ex.\ $N\!\approx\!250$, $K_1\!=\!25$, $K_2\!=\!5$
amb la configuració per defecte).}
\label{fig:arch}
\end{figure}

\section{Metodologia}

\subsection{Agregació per Pacient (MIL)}
\label{sec:mil}

El diagnòstic és per \textbf{pacient}, no per làmina. Una mateixa secció histològica pot no presentar senyals
visibles de metàstasi si s'analitza aïlladament, mentre que en una altra secció del mateix pacient sí que és
apreciable. Si s'entrena a nivell de làmina, les seccions N1 de pacients N1 que no mostren metàstasi visible
reben un senyal de supervisió incorrecte (N0), contaminant l'entrenament.

Per solucionar-ho, s'adopta un paradigma d'\textbf{aprenentatge per múltiples instàncies}
(\textit{Multiple Instance Learning}, MIL) \cite{ilse2018attentionbaseddeepmultipleinstance}: cada pacient és
un \textit{bag} i les seves làmines (instàncies) comparteixen l'etiqueta del \textit{bag}. El model aprèn a
classificar el \textit{bag} a partir de les instàncies.

Donades les probabilitats $\{p_i = P(\text{N1}_i)\}_{i=1}^{S}$ de les $S$ làmines d'un pacient, s'implementen
les estratègies d'agregació següents:

\begin{itemize}
    \item \textbf{Noisy-OR} (per defecte): $P(\text{N1}_{p}) = 1 - \prod_{i=1}^{S}(1 - p_i)$. Modela la
    hipòtesi que n'hi ha prou que una làmina presenti metàstasi per classificar el pacient com N1.
    \item \textbf{Max}: $P(\text{N1}_{p}) = \max_i\, p_i$. Predicció conservadora basada en l'evidència
    màxima.
    \item \textbf{Mean / LSE}: mitjana aritmètica o \textit{log-sum-exp} per als casos intermedis.
    \item \textbf{Attention MIL}: \texttt{PatientAggregator} basat en l'atenció \textit{gated} de Ilse et
    al.\ \cite{ilse2018attentionbaseddeepmultipleinstance}. Aprèn un pes d'atenció per a cada làmina a
    partir del seu embedding (produït per l'encoder del GAT sense el capçal MLP), i la representació del
    pacient s'obté com la suma ponderada dels embeddings de les seves làmines.
\end{itemize}

\subsection{Desbalanceig de Classes}

El conjunt de dades presenta un fort desbalanceig: els casos N0 superen àmpliament els N1 (proporció aproximada
80/20). Per combatre-ho s'apliquen tres mesures complementàries.

\textbf{(1) Divisió estratificada.} La partició entrenament/validació (80/20) es realitza de forma
estratificada a nivell de pacient: s'assegura que la proporció N0/N1 sigui equivalent en ambdós subconjunts.
Sense estratificació, amb un conjunt petit i desbalancejat, el conjunt de validació podria contenir per
atzar una proporció de N1 molt diferent a la real, invalidant les mètriques.

\textbf{(2) Pesos de classe.} S'apliquen pesos balancejats $w_c = N / (C \cdot n_c)$, on $n_c$ és el
nombre de pacients de la classe $c$, incorporats directament a la funció de pèrdua BCE. Així, cada error
sobre un cas N1 té més pes en el gradient que un error sobre N0, penalitzant més els falsos negatius.

\textbf{(3) Mostratge ponderat per \textit{batch}.} Durant l'entrenament s'utilitza un
\textit{WeightedRandomSampler} que assigna a cada pacient una probabilitat de mostratge inversament
proporcional a la freqüència de la seva classe. Això garanteix que cada \textit{mini-batch} contingui una
proporció equilibrada de casos N0 i N1, evitant que el model vegi gairebé exclusivament N0 durant molts
\textit{batches} consecutius.

\subsection{Grid Search i Configuració d'Entrenament}

S'ha dut a terme un \textit{grid search} amb múltiples hiperparàmetres variables: \textit{hidden units}, nombre
de caps d'atenció, tipus de \textit{pooling}, mètode d'agregació MIL, nombre de clústers DiffPool,
\textit{dropout} i \textit{weight decay}. Cada combinació d'hiperparàmetres genera una \textit{run} independent
amb mètriques registrades a \textit{Weights \& Biases}. El millor model per AUC de validació es desa com a
\textit{checkpoint}. L'entrenament inclou \textit{early stopping} amb 15 epochs de \textit{warm-up} i
\textit{patience} de 10, basant el criteri d'aturada en la pèrdua de validació.

\subsection{Atenció Supervisada amb Màscares}

Una línia d'investigació planificada és l'ús de les màscares binàries d'anotació expert (displàsia, infiltració)
per supervisar els pesos d'atenció del model GAT. L'objectiu és que les regions anotades amb patologia actuïn com
a guia de supervisió feble, forçant el model a focalitzar l'atenció sobre les àrees d'interès clínic
independentment de l'etiqueta N0/N1 final. Això permetria aprofitar les anotacions disponibles més enllà del
diagnòstic binari, afegint un senyal de regularització espacialment informat.

\section{Resultats Preliminars}

El \textit{grid search} inicial ha identificat un model candidat amb els hiperparàmetres de la
Taula~\ref{tab:hparams}. Les mètriques de validació a nivell de pacient es presenten a la
Taula~\ref{tab:metrics} i la Taula~\ref{tab:cm}.

\begin{table}[h]
\centering
\caption{Hiperparàmetres del model candidat.}
\label{tab:hparams}
\small
\begin{tabular}{lc}
\toprule
\textbf{Paràmetre} & \textbf{Valor} \\
\midrule
\textit{Hidden} ($d_h$) & 128 \\
Caps d'atenció ($H$) & 2 \\
\textit{Pooling} & \textit{diff} \\
Clústers DiffPool ($K$) & 5 \\
\textit{Dropout} & 0{,}5 \\
Agregació pacient & \textit{mean} \\
Millor epoch & 34 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Mètriques de validació del model candidat. AUC: \textbf{0{,}846}.}
\label{tab:metrics}
\small
\begin{tabular}{lccc}
\toprule
 & \textbf{N0} & \textbf{N1} & \textbf{Mitjana} \\
\midrule
Precisió  & 0{,}867 & 0{,}625 & 0{,}746 \\
Recall    & 0{,}813 & 0{,}714 & 0{,}763 \\
F1-Score  & 0{,}839 & 0{,}667 & 0{,}753 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Matriu de confusió (validació, nivell de pacient).}
\label{tab:cm}
\small
\begin{tabular}{cc|cc}
\toprule
& & \multicolumn{2}{c}{\textbf{Predicció}} \\
& & \textbf{N0} & \textbf{N1} \\
\midrule
\multirow{2}{*}{\textbf{Real}}
  & N0 & \cellcolor{green!20}26 & \cellcolor{red!15}4 \\
  & N1 & \cellcolor{red!15}4  & \cellcolor{green!20}10 \\
\bottomrule
\end{tabular}
\end{table}

L'AUC de 0{,}846 sobre 44 pacients de validació (30 N0, 14 N1) és un resultat prometedor tenint en compte el
desbalanceig de classes i la dificultat intrínseca del problema. El model mostra una millor capacitat de
discriminació per a N0 (recall 0{,}813) que per a N1 (recall 0{,}714), fet esperable per la menor representació
dels casos positius. Cal destacar que tan sols 4 falsos positius i 4 falsos negatius sobre 44 pacients indiquen
un bon potencial clínic.

\section{Implementació i Frontend}

La implementació segueix un disseny modular: la construcció de grafs, el model, el codi d'entrenament i els
\textit{data loaders} estan desacoblats per facilitar l'experimentació independent. La configuració
d'hiperparàmetres es gestiona via fitxer YAML, on qualsevol paràmetre pot ser una llista per activar el
\textit{grid search} automàtic. El repositori és accessible a \cite{morillo2026codi} i el desplegament al
servidor CVC es gestiona via GitHub Actions amb un \textit{runner self-hosted}.

\subsection{Frontend Web Interactiu}

S'ha desenvolupat un \textit{frontend} web (FastAPI + JavaScript pur, sense \textit{build step}) per facilitar
l'exploració dels models entrenats. Les funcionalitats principals inclouen:

\begin{itemize}
    \item \textbf{Inferència en temps real}: selecció de pacient o làmina, \textit{forward pass} al model actiu
    i visualització del resultat N0/N1 amb la descomposició per làmina.
    \item \textbf{Visualització de l'atenció}: representació del graf de Delaunay sobre la làmina, amb nodes
    colorejats per pes d'atenció (capes GATConv) i visualització dels \textit{patches} RGB originals.
    \item \textbf{Estadístiques}: corbes ROC i PR, matriu de confusió i mètriques per classe sobre el conjunt
    de validació.
    \item \textbf{Selecció de checkpoint}: comparació entre múltiples models entrenats.
\end{itemize}

La figura~\ref{fig:pipeline} mostra el diagrama de flux complet del sistema.

\begin{figure*}[h]
\centering
\begin{tikzpicture}[
    node distance=0.35cm and 0.22cm,
    box/.style={rectangle, rounded corners=3pt, draw,
                minimum width=1.9cm, minimum height=0.65cm,
                align=center, font=\small},
    ext/.style={box, draw=gray!55, fill=gray!12},
    own/.style={box, draw=blue!65!black, line width=1.3pt, fill=blue!7},
    % compact nodes for model row
    cmp/.style={rectangle, rounded corners=3pt, draw=blue!65!black, line width=1pt,
                fill=blue!7, minimum width=1.45cm, minimum height=0.60cm,
                align=center, font=\scriptsize},
    dpl/.style={rectangle, rounded corners=3pt, draw=orange!70!black, line width=1pt,
                fill=orange!10, minimum width=1.1cm, minimum height=0.60cm,
                align=center, font=\scriptsize},
    arr/.style={->, >=stealth, thick},
    lbl/.style={font=\small\bfseries, align=right},
]

%% --- Carril 1: Preparació de dades (gris) ---
\node[ext] (wsi)  {WSI};
\node[ext, right=of wsi] (sw)   {\textit{Sliding}\\window};
\node[ext, right=of sw]  (pat)  {Patches\\$256{\times}256$};
\node[ext, right=of pat] (bag)  {\textit{Bag}\\$16{\times}16$\\(4096\,px)};
\node[ext, right=of bag] (uni)  {\textit{UNI2}};
\node[ext, right=of uni] (cls)  {\textit{CLS}\\1536-dim};
\draw[arr] (wsi)--(sw); \draw[arr] (sw)--(pat);
\draw[arr] (pat)--(bag); \draw[arr] (bag)--(uni); \draw[arr] (uni)--(cls);

%% --- Carril 2: Construcció del graf (blau) ---
\node[own, below=1.9cm of wsi] (coo) {Coord.\\$(i,j)$};
\node[own, right=of coo]       (del) {Delaunay};
\node[own, right=of del]       (pru) {Poda\\$>2{\times}\bar{d}$};
\node[own, right=of pru]       (gra) {\textbf{Graf}\\$\mathbf{X}$\,[N${\times}$1536]\\$\mathbf{A}$\,[N${\times}$N]};
\draw[arr] (coo)--(del); \draw[arr] (del)--(pru); \draw[arr] (pru)--(gra);
\draw[arr] (bag.south) to[out=270,in=90] (coo.north);
\draw[arr] (cls.south) to[out=270,in=70] (gra.north);

%% --- Carril 3a: Backbone GAT+DiffPool (nodes compactes) ---
\node[cmp, below=2.0cm of coo] (g1) {GAT$_1$\\$N{\to}N$};
\node[dpl, right=0.18cm of g1] (p1) {DP$_1$\\$N{\to}K_1$};
\node[cmp, right=0.18cm of p1] (g2) {GAT$_2$\\$K_1{\to}K_1$};
\node[dpl, right=0.18cm of g2] (p2) {DP$_2$\\$K_1{\to}K_2$};
\node[cmp, right=0.18cm of p2] (g3) {GAT$_3$\\$K_2{\to}K_2$};
\draw[arr] (g1)--(p1); \draw[arr] (p1)--(g2);
\draw[arr] (g2)--(p2); \draw[arr] (p2)--(g3);
\draw[arr] (gra.south) to[out=255,in=90] (g1.north);

%% --- Carril 3b: Readout + MLP + predicció ---
\node[own, below=0.6cm of g1] (ro) {\textit{Readout}\\mean\,$\|$\,max};
\node[own, right=of ro]        (ml) {MLP};
\node[own, right=of ml]        (pr) {P(N1)\\secció};
\draw[arr] (g3.south) to[out=270,in=0] (ro.east);
\draw[arr] (ro)--(ml); \draw[arr] (ml)--(pr);

%% --- Agregació pacient ---
\node[own, below=0.8cm of ml]  (agg)  {Agregació\\MIL pacient};
\node[own, right=of agg]       (diag) {N0\,/\,N1\\pacient};
\draw[arr] (pr.south) to[out=270,in=30] node[right, font=\scriptsize] {${\times}S$} (agg.north east);
\draw[arr] (agg)--(diag);

%% --- Validació ---
\node[ext, below=0.8cm of diag] (exc) {Excel\\(\textit{ground truth})};
\draw[arr] (exc.north)--(diag.south);

%% --- Etiquetes de carril ---
\node[lbl, left=0.25cm of wsi, anchor=east]
    {\footnotesize Preparació\\de dades};
\node[lbl, left=0.25cm of coo, anchor=east]
    {\footnotesize Construcció\\del graf};
\node[lbl, left=0.25cm of g1,  anchor=east]
    {\footnotesize Model};

\end{tikzpicture}
\caption{Diagrama de flux del \textit{pipeline} complet. \textit{Blocs grisos}: etapes realitzades
pels companys de l'equip (extracció de \textit{patches} $256{\times}256$\,px agrupats en \textit{bags}
de $16{\times}16$ = $4096{\times}4096$\,px, sense filtratge, i obtenció de l'embedding CLS via UNI2).
\textit{Blocs blaus (vora gruixuda)}: contribucions d'aquest TFG. El model alterna capes GAT amb
capes DiffPool intercalades: la primera DiffPool redueix el graf de $N$ nodes a $K_1$ super-nodes i
la segona a $K_2 \ll K_1$; un \textit{readout} final agrega els $K_2$ nodes en un vector global. Com
a alternativa sense pooling jeràrquic, s'aplica directament \textit{mean} o \textit{max} sobre la
darrera capa GAT. La fletxa ${\times}S$ indica que les $S$ seccions d'un pacient es processen com a
grafs independents i les probabilitats P(N1) s'agreguen via MIL per obtenir el diagnòstic final
(Model 1); el Model 2 (mega-graf, Secció~\ref{sec:model-dades}) és una alternativa sense etapa
d'agregació.}
\label{fig:pipeline}
\end{figure*}

\printbibliography

\end{document}
